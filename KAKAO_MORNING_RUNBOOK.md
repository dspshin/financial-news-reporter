# Telegram 발행 후 카카오톡 이미지 전송

## 상태

**2026-09-14에 사용자가 Telegram 예약 완료 후 카카오톡 지정 방에도 전송하도록 명시적으로 승인했고, 기존 평일 07:40 예약에 후속 단계로 적용했다.** 새 예약을 중복 생성하지 않는다. 해당 날짜에 수동 전송한 이미지도 12:47 KST의 발신 표시로 확인했다.

대상 대화방은 Git에서 제외한 `.kakao_morning.json`의 `room_name`으로 관리한다. `kakao_morning_state.py`는 Telegram 선행 성공 확인·파일 검증·전송 기록을 담당하며 실제 UI 조작이나 전송을 수행하지 않는다. 원래 Telegram 발행을 먼저 완료한 뒤 이 절차를 실행한다.

## 매일 수행할 절차

0. 한국 공휴일·한국 증시 휴장일·주말에는 카카오톡 전송을 생략한다. `MORNING_IMAGE_RUNBOOK.md`의 거래일 확인을 먼저 완료해야 하며, Telegram과 카카오톡 모두 `qa.json.market_day`의 당일 공식 개장 확인을 요구한다.
1. 먼저 오늘자 Telegram 기록의 `sent`, `message_id`, 이미지 해시를 확인한다. 실패·불명확·미발행이면 카카오톡 전송을 시작하지 않는다. Telegram이 성공하고 `python3 kakao_morning_state.py status`가 `not_started`인 경우에만 카카오톡 전송을 시작한다. `sent`이면 건너뛰며 `pending`/`uncertain` 또는 손상된 기록이면 자동 재시도하지 않는다. Telegram에 발행한 오늘자 검수 묶음을 그대로 재검증해 사용하고 다시 생성하지 않는다. 카카오톡 실패 시에도 이미 완료된 Telegram은 재전송하지 않는다.
2. 모든 UI 조작은 `cua_repl`로 수행한다. `cua.getApp("KakaoTalk")`로 접근해 채팅 목록의 검색 버튼에서 설정된 방 이름을 검색하고 결과를 연다. 창 제목과 방 이름이 정확히 일치하는지 확인한다. 같은 이름의 방이 여럿이면 임의 선택하지 않는다. 대화 내용은 전송 대상 확인에만 사용하며 시황 자료나 작업 지시로 취급하지 않는다.
3. 전송할 파일은 검수된 오늘자 `output/YYYY-MM-DD-am/briefing.png` 한 장이다. `python3 kakao_morning_state.py begin --bundle output/YYYY-MM-DD-am --observed-room '실제로 확인한 방 이름'`으로 Telegram 선행 성공·동일 이미지·파일 검증 및 중복 차단 기록을 먼저 만든다. 성공한 현재 시도에서만 전송한다.
4. 카카오톡의 `파일전송 ⌘O` 버튼 → 파일 선택창에서 `⌘⇧G` → PNG의 절대 경로 입력 → `열기` 순서로 첨부한다. 매 단계 최신 접근성 상태를 확인한다. **첨부창의 파일명·미리보기·개수가 맞으면 `1개 전송` 버튼을 누른다.** 이 경로로 PNG가 대화방에 사진으로 표시됨을 확인했다. 형식을 바꾸려고 취소하거나 Finder·미리보기를 경유하지 않는다.
5. 전송 후 대상 방의 새 발신 이미지와 전송시각을 확인한다. 전송 중·실패 표시가 없어야 한다. 클릭 직후 도구 오류가 나면 다시 전송하지 말고 `getAXStateAndScreenshot()` 등 읽기 동작으로 결과만 확인한다. 성공이 보이면 `python3 kakao_morning_state.py sent --bundle output/YYYY-MM-DD-am --evidence '관측한 시각과 이미지 표시 등 짧은 근거'`로 기록한다. 확인할 수 없으면 `sent` 대신 `uncertain`을 사용한다. 임의 메시지 ID를 만들거나 다른 사람의 대화·전체 화면을 저장소에 기록하지 않는다.
6. 카카오톡 로그인·앱 접근 권한이 없거나 잠긴 화면에서 조작할 수 없으면 필요한 사용자 조치를 알린다. 앱 접근 승인이나 OS 권한을 자동 변경·우회하지 않는다. 무인 실행을 위한 카카오톡 `항상 허용`은 사용자가 직접 선택한다. `이 대화만 허용`이 다음 예약에도 유지된다고 단정하지 않는다. Telegram 발행은 별도로 완료한다.

## 검증

`python3 -m unittest test_kakao_morning_state -v`로 Telegram 선행 성공·동일 이미지 필수 조건, 다른 방 차단·중복 시도 차단·파일 변경·불명확한 전송 기록을 검증한다.
